# Frontend (Vite + React + TypeScript) — Starter

Setup

```bash
cd frontend
npm install
npm run dev
```

Notes
- Files added: `src/main.tsx`, `src/App.tsx`, `src/pages/Home.tsx`, demo UI components, `tokens.css`, `components.css`, `tokens.json`, `tailwind` config, `store` and `queries` examples.
- Tailwind is configured; ensure you run `npm install` to get dependencies.
